{
  "name": "poto-demo-app",
  "version": "1.1.0",
  "description": "Minimal demo application showcasing Poto framework",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "server": "bun run src/server.ts",
    "server-dev": "bun --hot run src/server.ts",
    "client": "bun run src/client.ts",
    "test": "bun test",
    "test:reactive": "bun test src/ReactiveState.test.ts",
    "poto:dev": "rm -rf node_modules/poto && ln -s $(cd .. && pwd) node_modules/poto && echo '✅ Switched to DEV mode (symlink to local poto)'",
    "poto:prod": "rm -rf node_modules/poto && bun install && echo '✅ Switched to PROD mode (installed from package.json)'",
    "poto:status": "if [ -L node_modules/poto ]; then echo '🔧 DEV mode: symlink to' && readlink node_modules/poto; else echo '📦 PROD mode: installed package'; fi"
  },
  "dependencies": {
    "poto": "https://github.com/iclass/poto/releases/download/v1.0.13/poto.tar.gz",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "valtio": "^2.1.8"
  },
  "devDependencies": {
    "@types/bun": "^1.2.23",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.1"
  }
}
